kind: entity_set_link
schema:
  url: "umodel.aliyun.com"
  version: "v0.1.0"
metadata:
  name: "code.module_implements_apm.service"
  domain: code
  display_name:
    en_us: "Module implements Service"
    zh_cn: "模块实现服务"
  description:
    en_us: "A code module implements a service (bridge edge)."
    zh_cn: "代码模块实现一个服务（桥边）。"
spec:
  src:
    domain: code
    kind: entity_set
    name: code.module
  dest:
    domain: apm
    kind: entity_set
    name: apm.service
  entity_link_type: implements
